# -*- coding: utf-8 -*-
"""Track A additions: accounts, saved profiles, the catalogue, the assistant.

Nothing in this package is imported by ``backend/main.py`` or by the engine.
It is bolted on in ``backend/app.py``, which is what the server actually runs,
so the Phase 1-2 backend stays byte-for-byte what its tests were written
against (docs/03-BUILD-BRIEFS.md Track A: "Change nothing that exists").
"""
